# gemini.md

## Purpose

This is the current context note for Gemini-family agents working in `lotto---webapp`.
Use it as the fast-start reference for the current structure and workflow.

- Date: `2026-05-04`
- Static data latest draw: `1221`
- Static data rows: `1220`
- Missing draw: `146`
- Current functional review artifact: `FUNCTIONAL_GAP_AND_COPY_REVIEW_2026-05-04.md`

## Current Snapshot

- App type: no-build SPA
- Entry flow:
    - `index.html`
    - `assets/modules/index.js`
    - `assets/modules/core/LottoApp.js`
- PWA bootstrap:
    - `assets/modules/bootstrap/pwa.js`
- Service worker cache version:
    - `v20`
- Service worker precache manifest:
    - `assets/sw-precache-manifest.js` generated by `scripts/generate_sw_manifest.mjs`
- Recent consistency fixes:
    - proxy setting changes abort any in-flight sync before queuing the replacement/default sync path
    - draw date normalization accepts only valid `YYYY-MM-DD` / official `YYYYMMDD` values
    - latest winning-card date/placeholder metadata is escaped before HTML rendering
    - strategy worker final timeout terminates the busy worker before fallback continues
    - transient static JSON failures preserve already-loaded in-memory winning data unless the caller disables preservation
    - import safety limits cap backup file size, projected ticket count, and oversized strategy snapshots
    - Cloudflare proxy `/proxy/latest` defaults to estimated latest draw when `draw_no` is omitted
    - opt-in browser happy-path and live sync checks were added: `npm run test:happy`, `npm run test:sync-live`
    - Prettier baseline is explicit via `.prettierrc.json` / `.prettierignore`
    - generator strategy selection is now driven by the selected strategy request; legacy toggles only act as quick presets
    - generated-number runtime state now preserves provenance (`numbers`, `strategyRequest`, `createdAt`, `source`)
    - sync runs are internally abortable for both manual and auto paths while public cancel remains manual-only
    - ticket-book duplicates now merge by `quantity`, including quantity-aware delete/import/campaign counts
    - import reconstructs `syncMeta` as `local_restore` only on successful winning-data rebuild, and records `local_restore_failed` on restore failure
    - `dataHealth` distinguishes `full` / `partial` / `none` using structural completeness
    - local updates are now evaluated as merged static + local data, so `static_local` can still be `partial` if intermediate draws are missing
    - sync payload draw numbers must be integer `>= 1`; decimal/zero/negative draw payloads are rejected
    - favorites/history import now uses central stored-number normalization and drops decimals, duplicates, and out-of-range values
    - imported ticket IDs are bounded and normalized to safe `[A-Za-z0-9_-]` characters
    - check-target card rendering escapes `data-item-key` and metadata before writing HTML attributes
    - storage summary accounting now uses UTF-8 bytes, not JavaScript string length
    - `package.json` declares `"type": "module"` to avoid Node ESM typeless package warnings
    - static data freshness and service-worker precache reachability are covered by smoke regressions
    - static JSON failure can fall back to local-only `winningStats` rebuild and partial recovery mode
    - `stats`, `ai`, and `bt` are gated when data availability is not full
    - service-worker multi-tab reload is broadcast only after activation, not immediately on update click
    - app-owned localStorage keys now sync across tabs via `BroadcastChannel('lotto-data-sync')` with `storage` fallback
    - `reconcileTicketChecks()` revalidates stored `checked` tickets after sync/import/local-update cleanup
    - future local updates are sanitized, deduped, and capped at `estimateLatestDrawKST() + 2`
    - `syncMeta.lastSuccessDrawNo` now clamps to effective winning data
    - orphan campaigns are pruned after import, ticket delete / ticket-book clear, and cold-start load normalization
    - history now preserves duplicate actual-log entries instead of number-unique snapshots
    - immediate settlement for already-drawn tickets
    - generator campaign reset restores target-draw auto-follow metadata
    - Android PWA no-seed generation and recommendation now mix runtime entropy, avoiding fixed repeat results after install/cache reuse
    - user-facing recommendation copy is `번호 추천`; advanced numeric controls sit under `고급 설정` with `빠름/기본/정밀` analysis presets
    - settings and latest draw card show `내 데이터 / 예상 최신 / 차이`, backed by `npm run check:data-freshness`
    - PWA update handling exposes `window.lottoPwaUpdate` and dispatches `lotto:pwa-update-state` for settings-button state
    - import uses a preview-confirm flow; overwrite (`바꾸기`) downloads `lotto_before_replace_*.json` before applying
    - `cleanupStoredRecords()` powers `백업하고 정리하기` and keeps local latest-draw corrections out of automatic cleanup
    - build now runs `lint -> check:data-freshness -> smoke`, and `npm run test:pwa-mobile` covers Android-like PWA non-fixed generation/recommendation
- Styles:
    - `assets/app.css` is the aggregate entrypoint
    - actual style slices live in `assets/styles/*.css`
- Split core internals:
    - `assets/modules/core/app/`
    - `assets/modules/core/app/moduleLoader/`
    - `assets/modules/core/app/dataLists/`
    - `assets/modules/core/data/`
    - `assets/modules/core/data/records/`
    - `assets/modules/core/data/persistence/`
    - `assets/modules/core/data/sync/`
    - `assets/modules/core/ui/`
    - `assets/modules/core/strategy/`
- Split feature internals:
    - `assets/modules/features/ai/`
    - `assets/modules/features/backtest/`
    - `assets/modules/features/check/`
    - `assets/modules/features/dataio/`
    - `assets/modules/features/generator/`
- Smoke layout:
    - `scripts/smoke/helpers/`
    - `scripts/smoke/cases/`
    - `scripts/smoke/cases/regressions/`
    - `scripts/smoke/cases/regressions/manifest.mjs`
    - `scripts/smoke/cases/regressions/support.mjs`
    - `scripts/smoke/smoke.mjs`
- AI strategy additions:
    - richer context + reranking in `assets/modules/core/strategy/`
    - stable additions: `consensus_portfolio`, `bayesian_smooth`, `momentum_recent`, `mean_reversion_cycle`
    - AI-only automatic modes: `auto_recent_top`, `auto_ensemble_top3`

## UX Notes

- Storage, advanced data connection, sync, alert, and theme settings are handled from the global settings modal.
- On mobile, the settings modal is intentionally rendered as a single-column sheet.
- `UIManager` now provides common confirm/prompt modals with focus trap and focus restore.
- The data page is focused on backup/import and list management.
- Data list rendering is aligned with actual search/pagination state again.
- Data list search/page state is persisted in `sessionStorage` and the page also exposes local update summary/cleanup UI.
- Generate/campaign actions now guard against duplicate runs and stale async result overwrites.
- Resetting campaign options restores the target-draw auto-follow metadata rather than only resetting the raw input value.
- Backtest results persist across route re-entry and render mini metric charts.
- Saving a past-draw ticket settles it immediately if the winning draw is already available locally.
- The check tab now uses a card list with search, ticket-status filtering, keyboard navigation, and always-visible scanned results.
- Mobile bottom navigation is now `gen/stats/ai/check/data + more`, and install entry points are mirrored to desktop/settings/mobile more.
- Target draw inputs (`genTargetDrawNo`, `campStartDraw`, `aiTargetDrawNo`) auto-follow the next draw until manually edited.
- Each target draw input has a reset action to restore the suggested next draw.
- Latest draw sync defaults to automatic fallback.
- A configured advanced data connection is preferred only when it matches the official `/proxy/latest` contract.
- Unsupported proxy formats are ignored at runtime and surfaced as warnings in settings.
- If no advanced data connection is set, the app still attempts runtime sync and falls back to bundled static JSON plus local device corrections on failure.
- `data/winning_stats.json` is install-precached for offline stability.
- same-origin network reachability probe now uses `/online-check.txt`, which the SW deliberately bypasses from cache interception.
- Invalid single-draw payload shapes now emit `SYNC_FETCH_ONE_INVALID_PAYLOAD` and are surfaced via `syncMeta.lastWarningMessage`.
- Merge/overwrite (`합치기/바꾸기`) import previews the impact, prunes orphan campaigns, and includes cleanup counts in the completion toast.
- Ticket delete / clear flows also prune orphan campaigns and append cleanup counts to success feedback.
- Clearing local updates rebuilds `winningStats`, clamps sync metadata, and resets stale `checked` tickets back to pending when needed.
- `refreshCurrentRoute()` applies a stale guard so async refresh work from an old route does not render after a tab switch.
- Leaving the `check` route stops the QR scanner, and clicking the scanner backdrop closes it.
- AI recommendations now also:
    - rerank a candidate pool before final selection
    - surface recommendation score / pair synergy / profile fit / gap balance diagnostics
    - use `aiLookbackWindow` as the recent `N` window when automatic AI-only strategies are selected

## Key Map

- `index.html`
    - page shell, settings modal, mobile more sheet, dialog modal
- `assets/modules/index.js`
    - app entrypoint
- `assets/modules/bootstrap/pwa.js`
    - service worker registration, activation-ordered update UX, and `window.lottoPwaUpdate`
- `assets/modules/core/LottoApp.js`
    - facade, implementation in `core/app`; target-draw auto-management, mobile more/install sync, and remote persistence rehydrate entry live here
- `assets/modules/core/UIManager.js`
    - toast + shared dialog modal + focus trap + accessible ball rendering
- `assets/modules/core/DataManager.js`
    - facade, implementation in `core/data`
- `assets/modules/core/app/latestDraw.js`
    - latest draw card refresh + target-draw autofill sync
- `assets/modules/core/app/moduleLoader.js`
    - barrel for route stale guard, route data gate, and request bridge logic
- `assets/modules/core/app/dataLists.js`
    - barrel for list state/render/pagination/events
- `assets/modules/core/app/settingsPanel.js`
    - sync warning metadata, data freshness copy, PWA update state, and `local_restore_failed` badge/warning
- `assets/modules/core/data/records.js`
    - ticket `quantity` grouping, actual-log history merge, orphan-campaign pruning, and stored-record cleanup helpers
- `assets/modules/core/data/analytics.js`
    - `reconcileTicketChecks()` for whole-ticket settlement revalidation
- `assets/modules/core/data/persistence.js`
    - local-update sanitization, `local_restore` / `local_restore_failed` sync-meta handling, and sync-meta clamping
- `assets/modules/core/data/persistence/storage.js`
    - BroadcastChannel/storage-event based cross-tab sync
- `assets/modules/core/data/persistence/loadSave.js`
    - cold-start orphan campaign cleanup before persist
- `assets/modules/core/data/sync.js`
    - data-health classification, partial recovery, single-draw payload diagnostics, sync warning tracking, and post-refresh ticket reconciliation
- `assets/modules/core/data/sync/health.js`
    - structural completeness checks tied to `CONFIG.LIMITS.MISSING_DRAWS`
- `assets/modules/features/Check.js`
    - facade; implementation split into `features/check/events.js`, `list.js`, `results.js`
- `assets/modules/features/backtest/ui.js`
    - barrel for `features/backtest/events.js`, `rendering.js`, `strategyForm.js`
- `scripts/smoke/cases/regressions/manifest.mjs`
    - regression execution order and shared pass labels
- `scripts/smoke/cases/regressions/support.mjs`
    - shared smoke imports/utilities reused by domain-specific regression modules
- `assets/modules/features/generator/form.js`
    - campaign reset restores target-draw auto-follow metadata
- `assets/modules/features/dataio/support.js`
    - orphan-campaign pruning helpers
- `assets/modules/features/dataio/importExport.js`
    - import preview/confirm/apply flow, overwrite pre-backup, and cleanup-count toast
- `assets/modules/utils/analysisPresets.js`
    - beginner-friendly `빠름/기본/정밀` analysis presets shared by generator/recommendation/backtest forms
- `assets/modules/utils/dom.js`
    - shared HTML escaping for saved/imported/external data inserted into templates
- `assets/modules/utils/strings.js`
    - centralized user-facing strings
- `assets/modules/core/StrategyEngine.js`
    - facade, implementation in `core/strategy`
- `assets/modules/core/strategy/context.js`
    - richer recent-history stats including pair matrices and gap distributions
- `assets/modules/core/strategy/weights.js`
    - base weights and adaptive recent-performance auto-strategy logic
- `assets/modules/core/strategy/generation.js`
    - reranking and diversity-aware candidate selection
- `assets/modules/features/*.js`
    - public entry files kept stable
- `sw.js`
    - generated precache manifest import, online-check bypass, and fetch strategy
- `scripts/generate_sw_manifest.mjs`
    - precache manifest generator
- `scripts/tests/offline_playwright.mjs`
    - opt-in browser scenarios for offline gates and cross-tab auto-rehydrate
- `scripts/tests/happy_playwright.mjs`
    - opt-in browser happy path for generate -> ticket -> check, recommendation import, and backup import
- `scripts/tests/pwa_mobile_playwright.mjs`
    - Android-like viewport/user agent PWA scenario ensuring no-seed generation/recommendation do not stay fixed
- `scripts/tests/sync_live.mjs`
    - opt-in live official draw payload check through the built-in sync path
- `scripts/check_static_data_freshness.mjs`
    - static data freshness budget check used by build
- `FUNCTIONAL_GAP_AND_COPY_REVIEW_2026-05-04.md`
    - current PWA, functional gap, and beginner-copy review plus implementation status
- `FUNCTIONAL_IMPLEMENTATION_AUDIT_2026-04-30.md`
    - prior functional implementation audit; superseded where the 2026-05-04 review says so

## Storage / Sync

Main keys:

- `lotto_pro_fav_v2`
- `lotto_pro_hist_v2`
- `lotto_pro_settings_v2`
- `lotto_pro_ticketbook_v1`
- `lotto_pro_campaigns_v1`
- `lotto_pro_alerts_v1`
- `lotto_pro_strategy_presets_v1`
- `lotto_pro_sync_meta_v1`
- `lotto_pro_updates_v2` (`CONFIG.KEYS.LOCAL_UPDATES`)

Static winning data:

- `data/winning_stats.json`
- current max draw: `1221`
- current rows: `1220`
- allowed missing draw: `[146]`

Runtime-only data health:

- `availability`: `full | partial | none`
- `source`: `static | static_local | local_only | none`
- `static_local` can be `partial` when merged local updates leave intermediate draw gaps

Important sync modes:

- `local_restore`
- `local_restore_failed`

Advanced data connection priority:

1. `?proxyUrl=` / `?proxy=`
2. legacy `lotto_webapp_settings_v1.proxyLatestUrl`
3. `lotto_pro_settings_v2.customProxy`
4. otherwise automatic fallback sync

Official supported advanced connection shape:

- absolute `http(s)` URL
- path contains `/proxy/latest`
- unsupported shapes (`?url=`, `{url}`, `{draw_no}`) are ignored at runtime
- `syncMeta` also stores recent warning diagnostics for invalid single-draw response shapes
- Proxy setting changes cancel the active in-flight sync before the replacement/default sync is queued.

Import limits:

- `MAX_IMPORT_BYTES`: `2MB`
- `MAX_IMPORT_TICKETS`: `2000`
- `MAX_STRATEGY_REQUEST_BYTES`: `8000`
- `MAX_SERIALIZABLE_DEPTH` / `MAX_SERIALIZABLE_KEYS` / `MAX_SERIALIZABLE_ARRAY_ITEMS`: strategy snapshot complexity guards

## Quick Commands

```bash
python -m http.server 5173
npm install
npm run sync:sw-manifest
npm run lint
npm run check:data-freshness
npm run build
node scripts/smoke/smoke.mjs
npm run format:check
node scripts/perf/bench.mjs
npm run bench:ai
npm run bench:ai:full
npm run test:happy
npm run test:offline
npm run test:pwa-mobile
npm run test:sync-live
```

- `npm run test:offline` requires a local `Chrome`/`Edge` channel or a Playwright-installed Chromium (`npx playwright install chromium`).
- `npm run test:happy` uses the same browser availability requirement and covers generate -> ticket -> check, recommendation -> generator import, and backup import merge preview.
- `npm run test:pwa-mobile` uses an Android-like browser profile with service-worker/PWA flow to ensure unseeded generation and recommendation results change across runs.
- `npm run test:sync-live` is an opt-in network check for the current/latest official draw payload.

Local URL:

- `http://localhost:5173/`

## Verification Priority

1. `npm run lint`
2. `npm run check:data-freshness`
3. `node scripts/smoke/smoke.mjs`
4. generator / recommendation / backtest basic flows
5. target draw autofill and reset behavior
6. settings modal state reflection, including data freshness, PWA update, and sync warning metadata
7. mobile settings modal and mobile more sheet rendering
8. check tab card list, search/filter state, and keyboard navigation
9. backup/import preview behavior
10. save a past-draw ticket and verify it settles immediately
11. reset campaign options and verify target-draw auto-follow resumes
12. backup/import including orphan-campaign cleanup toast counts and duplicate history preservation
13. clear local updates and verify stale checked tickets reset + applied draw clamps correctly
14. delete the last campaign-linked ticket and verify the orphan campaign disappears immediately
15. advanced data connection unset/set sync policy and invalid payload diagnostics
16. import restore success/failure state (`local_restore` / `local_restore_failed`)
17. data-page local update cleanup and `백업하고 정리하기` flow
18. common confirm modal flows for destructive actions and preset overwrite/delete
19. service worker update check, acceptance, and reload
20. two-tab auto rehydrate after ticket/settings change
21. merged local-update gap classification and static data freshness budget
22. import stored-list strict normalization and sync payload integer guard
23. check target-card attribute escaping
24. storage summary byte accounting
25. service-worker precache reachability
26. malformed draw date rejection and latest win date escaping
27. advanced data connection input change abort and transient static data preserve behavior
28. import safety limits
29. browser happy path, Android-like PWA mobile, and live sync opt-in checks

## Session Template

```md
### Session Handoff (Gemini)

- Changed files:
- Change goal:
- Data/storage impact:
- Worker contract impact:
- Verification completed:
- Remaining risks:
- Next recommended task:
```
